# Setup

- Make sure you're running ruby >= 2.4.0 (`rvm list`)
- Run `make` and pray.

# View blog

- `jekyll serve --drafts`

# Create a new post

- `bundle` first.
- `thor jekyll:new The title of the new post`
